name = "url-shortener"
main = "index.js"
compatibility_date = "2024-01-01"

# Secrets are not stored here - add them via Cloudflare dashboard or wrangler CLI
# See CLOUDFLARE_SECRETS.md for the list of required secrets
